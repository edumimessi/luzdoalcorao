/**
 * Configuração pública do site.
 * Para ativar o Google Analytics, substitua a string vazia por um ID "G-XXXXXXXXXX".
 * Nenhum rastreamento é carregado enquanto o campo permanecer vazio.
 */
export const SITE_CONFIG = Object.freeze({
  googleAnalyticsId: ""
});
